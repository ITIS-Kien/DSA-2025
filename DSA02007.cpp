#include <bits/stdc++.h>
using namespace std;
#pragma GCC optimize("Ofast")
#define BOOST ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0); 
#define ll long long
#define endl '\n'
const int MOD = 1e9 + 7;


int main(){
    BOOST;
    int t; cin >> t;
    while (t--)
    {
        int n; cin >> n;
        string s; cin >> s;
        int j = 0, ok = 0;
        while (n-- and ok) {   
            for (int i = j + 1, j < s.size(), ++i){
                if(s[i] > s[j]){
                    
                }
            }

        }
        
    }
       
    
}